import * as z from 'zod'

const formSchema = z.object({
  name: z.string().trim().min(1, 'Campo não pode estar vazio'),
  email: z.string().email(),
})

export default formSchema
export type FormInputs = z.infer<typeof formSchema>
